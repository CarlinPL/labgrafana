plugin: aws_ec2
regions:
  - sa-east-1


filters:
  instance-state-name: running

keyed_groups:
  - key: tags.role
    prefix: ""
    separator: ""

compose:
  ansible_user: "'ubuntu'"
  ansible_ssh_private_key_file: "'~/.ssh/pcgoku.pem'"